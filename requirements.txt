Pillow==10.0.0
pyperclip==1.8.2
imageio-ffmpeg==0.5.1

# Optional GPU-accelerated screen recording (Windows only). Recording still
# works everywhere without these - it just falls back to slower, CPU-based
# capture (also used automatically if DXGI duplication itself is refused,
# e.g. over Remote Desktop or on some virtual displays).
bettercam==1.0.0; sys_platform == "win32"
opencv-python-headless>=4.8; sys_platform == "win32"
